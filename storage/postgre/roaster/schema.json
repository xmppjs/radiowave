{
    "name": "roaster",
    "type": "postgresql",
    "versions": [{
        "version": "0.0.1",
        "description": "Data model for roaster",
        "statements": [
            "CREATE TABLE roaster (id SERIAL, jid TEXT, item JSON);",
            "CREATE INDEX ON roaster(jid);",
            "CREATE INDEX ON roaster((item->>'id'));"
        ]
    }]
}
